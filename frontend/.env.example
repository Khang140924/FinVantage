# Must match root USE_MOCK_AUTH (mock=true, cognito=false).
# Frontend auth requests stay relative at /auth; Vite proxies them to the local Auth BFF on localhost:4000.
VITE_AUTH_MODE=mock
VITE_API_BASE_URL=http://localhost:3000/dev
